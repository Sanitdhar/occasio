Closes #

## What changed

<!-- One paragraph. What does this do that the codebase could not do before? -->

## Why this way

<!-- Only where a reader would otherwise wonder: a tradeoff, a rejected alternative, a decision
     this implements (e.g. "D17 — tokens rather than literals"). Delete if it is obvious. -->

## Checks

- [ ] `npm run verify` passes locally
- [ ] Scoped to one issue — anything else was split out
- [ ] Title is in conventional-commit form (it becomes the squashed commit)
- [ ] New architectural rules, if any, have a probe in `tools/enforcement/`
